import Distribuidor.*
import Regalo.*
import Persona.*

describe "Gift Dealer" {

	test "Personas Inconformistas" {
		yoYoRussell.pintar("Azul")
		giftDealer.agregarRegalo(scalextric)
		giftDealer.agregarRegalo(balero)
		giftDealer.agregarRegalo(yoYoRussell)
		const inconformistas = giftDealer.inconformistas()
		assert.that(inconformistas.head().equals(stefan))
		assert.equals(1,inconformistas.size())
	}
}

